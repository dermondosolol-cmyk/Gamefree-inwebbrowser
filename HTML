<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nebula Gaming UI</title>
    <style>
        /* CSS3 Animations & Styling */
        :root {
            --primary: #00f2ff;
            --bg: #0b0e14;
            --card-bg: rgba(255, 255, 255, 0.05);
        }

        body {
            margin: 0;
            background: var(--bg);
            color: white;
            font-family: 'Segoe UI', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }

        /* Animated Background Glow */
        body::before {
            content: '';
            position: absolute;
            width: 300px; height: 300px;
            background: var(--primary);
            filter: blur(150px);
            opacity: 0.2;
            animation: moveGlow 10s infinite alternate;
        }

        @keyframes moveGlow {
            from { transform: translate(-50%, -50%); }
            to { transform: translate(50%, 50%); }
        }

        .container {
            background: var(--card-bg);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255,255,255,0.1);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            transition: transform 0.3s ease;
        }

        .container:hover {
            transform: translateY(-5px); /* Simple neat hover effect */
        }

        h1 {
            font-size: 2.5rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 20px;
            background: linear-gradient(to right, #fff, var(--primary));
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .btn {
            background: var(--primary);
            color: var(--bg);
            border: none;
            padding: 15px 40px;
            font-weight: bold;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
            text-transform: uppercase;
        }

        .btn:hover {
            box-shadow: 0 0 20px var(--primary);
            letter-spacing: 1px;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Nebula Quest</h1>
        <p>Experience the next generation of web gaming.</p>
        <button class="btn" onclick="startGame()">Start Game</button>
    </div>

    <script>
        // Interactive Logic (Replaces Java for Web UI)
        function startGame() {
            alert("Game Initializing... (JavaScript logic running)");
            // Add your game loop or state management here
        }
    </script>
</body>
</html>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nebula Gaming UI</title>
    <!-- CSS is typically internal or in a separate .css file -->
    <link rel="stylesheet" href="style.css"> 
</head>
<body>

    <div class="container" id="gameCard">
        <h1 id="mainTitle">Nebula Quest</h1>
        <p>Experience the next generation of web gaming.</p>
        <button class="btn" id="startBtn">Start Game</button>
        <div id="statusMessage" style="margin-top: 20px; color: var(--primary); opacity: 0;"></div>
    </div>

    <!-- CONNECTION TO EXTERNAL JAVASCRIPT -->
    <script src="script.js"></script>
</body>
</html>

